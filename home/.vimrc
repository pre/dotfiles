filetype plugin indent on
syntax on

set tabstop=2       " The width of a TAB (it's still a \t)
set shiftwidth=2    " Indent width
set softtabstop=2   " Number of columns for a TAB
set expandtab       " Expand TABs to spaces
